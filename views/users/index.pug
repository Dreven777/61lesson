doctype html
html
  head
    title= title
    link(rel="stylesheet", href="/styles.css")
  body
    h1= title
    ul
      each user in users
        li
          a(href=`/users/${user.id}`)= user.name
